---
name: friction
description: Record one atomic friction — the gap between what is and what could be — into the learning inbox (via the inbox MCP friction tool).
---

# friction

Capture the friction while it is fresh, well-formed and atomic, then call the
`friction` tool of the `inbox` MCP. Frictions are governance fuel.
