---
name: journal
description: Record the session — progress, problems, insights and context gaps — into the learning inbox (via the inbox MCP journal tool).
---

# journal

Record a session snapshot into the learning inbox. Follow the shape in the sibling
`format.md` file, then call the `journal` tool of the `inbox` MCP.
